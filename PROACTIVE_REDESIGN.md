# 主动消息改造（2026-09-19）

## 行为

- 启用后安排第一次检查，不再立即发消息。手动触发仍可用；它只绕过间隔/时段，不绕过正在聊天和连续追问上限。
- 默认本地时间08:00至次日01:00；起止相同为全天。闹钟和备用Worker使用同一绝对时间。后台延迟唤醒仍会重新检查时段。
- 默认20%轮次组装助手完整工具，80%只带已启用本机记忆的只读搜索和至多两个已配置MCP记忆搜索端点；可调0至100%。不支持工具的模型不带工具。两种模式沿用同一人格、近期历史、消息上限与模型输出预算。
- 后台工具遵守原有审批配置，需要当面确认的工具不执行；全自动许可仍按用户原设置。活动记录新增查看入口。没有新增全天自由循环、完整日记日历或付费缓存预热。
- 应用时间线默认关闭。只有主动查岗、系统工具中的AppUsage、自动时间线三项均开启且Android使用记录权限已授予才会读取。最多最近6小时24次前台切换，不读取屏幕/聊天内容，不新增Supabase表或上传行为。
- 后台先完成判定，只有SEND才将本轮消息写入聊天。WAIT/STOP和部分生成不会再短暂插入聊天流。

## 活动接续

本机账本按助手与会话隔离，每个会话最多64条。记录等待/停止/发送的结果、可选240字简记以及工具已返回的事实，不提取完整reasoning或保存原始工具结果。工具返回不等于业务成功。

Pending记录已持久化，而非等下次聊天才落盘。正常聊天在发给模型的副本中把记录接到对应用户回合；回复成功、没有未完成工具时才认领该快照。失败不消费，新到记录不会被顺带认领。已认领记录保持在原回合；不重复添加聊天气泡，也不自动写进长期记忆。

这部分不改变system前缀，但不能保证整个请求缓存命中：既有记忆、插件和其他动态注入仍有各自行为。64条上限意味着这不是永久档案；切设备、备份恢复和删除会话的生命周期还需单独验收。

## 验收清单

1. 关闭主动查岗/自动时间线：请求不含应用轨迹；开启三项但无系统权限时明确显示数据不可用，不推断正在做什么。
2. 手动触发，模型返回NOTE与WAIT：不弹消息；再次触发能看到前次简记。强制结束APP再打开后记录仍在。
3. 发送真实消息：请求包含pending记录；模拟网络失败后重试仍包含。成功后下一回合只在原用户回合带入，不再重复。
4. 两个助手/会话交替使用，记录和追问状态不串；重生成旧分支不消费新活动。
5. 设置普通白天时段和跨午夜时段，检查下次时间；在时段外仅手动触发可检查。两个调度源不重复发消息。
6. 完整工具比例0/100，核对实际工具schema；完整轮次工具取消/失败不得伪造业务成功。
7. 在主动生成期间发送用户消息：旧生成退出，不覆盖用户新消息，不留下半截主动消息。

## 验证范围

最终本地验收：app146+ai107共253项测试通过，lint0错误（仍有504条警告）、assembleDebug与APK签名验证通过。试用包`app/build/outputs/apk/debug/OrangeChat-Proactive-20260919-arm64.apk`，包名`me.rerere.orangechat.liquidglass`。源码未提交、未推送。

浏览器读取失败后用户提供了14张全文截图，已逐页核对。原文明确pending已保存，轻量可搜记忆；本版据此补齐只读搜索、独立活动查看，并保留成功回复后认领的更保守做法。未照搬iOS、PWA/APNs、每5分钟轮询或缓存预热。首次app139+ai107共246项测试通过，APK构建成功；全文校正后的结果见`.codex/STATE.md`。未连接Android真机，不能把本地通过等同于息屏推送、厂商保活或真实模型行为验收。
