# 额外注入缺项修复

## 2026-09-20 页面刷新验收与仓库构建
- 后续修复初进页灰色、滚动后才刷新的问题：列表描述按当前状态生成，设置页直接使用已加载设置，不额外创建默认快照。
- app173项单测通过；新增2项Compose界面回归已编译，未在设备执行。用户安装UIRefresh版本后明确反馈“效果很好”。
- 用户已授权提交并推送仓库构建。Build Personal APK保留普通Release产物OrangeChat-personal，并新增水玻璃Debug产物OrangeChat-liquidglass；二者应用包名不同。
- 下方为此前四项采集修复的阶段性记录；旧的“未提交”及APK信息不代表此次仓库构建产物。

## 本地完成
- 高德地址解析兼容 streetNumber 对象和可选文本的空数组，不再因为门牌结构丢掉整份地址。
- 地址查询与坐标读取分别记录结果；高德请求支持超时取消，失败不会拖住整轮采集。
- 当前应用优先读取活动/焦点应用窗口，再回退到当前窗口根节点；不使用过期应用记录冒充当前前台。
- OCR 截图限流时只重试一次；未连接无障碍、系统版本不支持、受保护屏幕及其他失败明确区分，不绕过系统限制。后台仍不截屏。
- 通知授权使用系统接口，兼容 Android 8.0 精确组件比对；区分授权与监听连接，断连请求系统重新绑定。近期通知保留本进程观察到的24小时历史，最多500条，注入最多5条；不会恢复进程启动前已消失的通知。
- 额外注入页新增当前安装版本的授权入口、高德 Web 服务 Key 输入和最近一次读取状态。诊断仅存状态，不存坐标、屏幕正文、通知或原始错误。
- 已开启来源失败时提供固定的不可用说明，不再静默消失。单项失败不阻断其他来源；相关记忆为空不等于故障。
- 保留上一轮设置更新锁修复，并修正主开关在初始设置加载前仍可点击的问题。

## 验证与产物
- 日志：D:/silence/scratch/extra-four-sources-20260919.log，BUILD SUCCESSFUL in 2m52s，进程exit0。
- app170 + ai107 = 277项测试全过，0失败/0错误/0跳过；包含10项新增解析、通知历史和诊断回归。
- assembleDebug通过，git diff --check通过，v2签名通过；DEX核验五个新增实现符号存在。
- APK：app/build/outputs/apk/debug/OrangeChat-ExtraInfo-Fix-20260920-arm64.apk。
- 包名：me.rerere.orangechat.liquidglass；版本2.2.3/159；arm64-v8a。
- SHA256：77508D9986A275512BC52646A7A9E3D85089FFBC47F9E5B175CB58D382DB7B3F。
- 未重跑lint；没有连接手机，未实测UI、权限状态、真实地址/OCR/通知。工作树未提交推送。

## 手机复测
1. 安装上述新包，不要使用旧ProviderExtra包。进入额外注入页，确认开关退出重进后保持。
2. 在该页“授权设置”为当前版本开启无障碍和通知访问；另一版本西瓜瓣的授权不等于当前版本已授权。返回后确认连接状态。
3. 详细地址需要有效的高德Web服务Key及定位权限；不应把Android端Key当Web服务Key。
4. 从普通聊天发送新一条消息，再查看额外注入页“最近一次聊天读取”。逐项记录成功、未授权、未连接、超时或空结果，不仅问模型有没有看到。
5. 当前屏幕文字是发起请求时的屏幕快照，不是发送前其他应用的历史画面；受保护页面不可截取，后台主动消息不采集OCR。
6. 用一条普通可读通知测试：监听连接后收到通知，再请求读取；划掉通知后仍应能读取同进程内24小时历史。系统/空白/持续通知仍按原规则过滤。

源码问题已修复不等于四项真机全部通过。若仍缺项，下一步依据最近读取状态定位，不继续猜手机权限。
